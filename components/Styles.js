import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    tabContainer: {
        backgroundColor: '#33ABA5', // Background color for the tab bar
      },
      activeTabLabel: {
        color: '#CCEAE9', // Color for the active tab label
        fontSize: 16,
        fontWeight: 'bold',
      },
      inactiveTabLabel: {
        color: '#FFFFFF', // Color for the inactive tab label
        fontSize: 16,
        fontWeight: 'bold',
      },
});

export default styles;
